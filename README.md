bobisme.github.io
=================

Static projects plus a Zola-powered blog.

Local development:

```bash
./scripts/serve.sh
```

Publish static files to repo root for GitHub Pages:

```bash
./scripts/publish.sh
```

This keeps `pgmm/` and `pgqp/` in place while updating the generated blog and homepage.
